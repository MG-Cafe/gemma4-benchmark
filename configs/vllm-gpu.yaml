# vLLM Configuration — GPU (RTX Pro 6000 Blackwell)
# 1x NVIDIA RTX PRO 6000, 96GB GDDR7, FP8 quantization

model: google/gemma-4-26B-A4B-it
host: "0.0.0.0"
port: 8000
dtype: bfloat16
quantization: fp8
gpu_memory_utilization: 0.95
max_model_len: 128000
max_num_batched_tokens: 65536
max_num_seqs: 8
enable_chunked_prefill: true
enable_prefix_caching: true

# Memory breakdown at this config:
#   Model weights (FP8): 25.67 GiB
#   KV cache: 53.86 GiB (235,296 tokens)
#   CUDA graphs + overhead: ~10 GiB
#   Total: 89.4 / 97.9 GiB (91.4%)

# Launch command:
# vllm serve google/gemma-4-26B-A4B-it \
#     --dtype bfloat16 --quantization fp8 \
#     --gpu-memory-utilization 0.95 --max-model-len 128000 \
#     --max-num-batched-tokens 65536 --max-num-seqs 8 \
#     --enable-chunked-prefill --enable-prefix-caching
